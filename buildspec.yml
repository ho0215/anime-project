version: 0.2
phases:
  install:
    commands:
      - echo "=== OS Release Info ==="
      - cat /etc/os-release
      - echo "=== Architecture ==="
      - uname -m
      - echo "=== Checking existing yum repos ==="
      - yum repolist
