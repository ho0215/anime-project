# Argo CD — 랩 클러스터 설치용 참고 매니페스트
# 설치는 공식 install.yaml 사용 (scripts/argocd-lab-install.sh).
# 이 디렉터리는 Application 등 GitOps 리소스만 둡니다.
apiVersion: v1
kind: Namespace
metadata:
  name: argocd
  labels:
    app.kubernetes.io/part-of: argocd
